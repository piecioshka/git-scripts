#!/usr/bin/env bash

set -e

repo=$(git config --get remote.origin.url)

# Replace ":" to "/"
repo="${repo/://}"

# Replace "git@" by "https://"
repo="${repo/git@/https://}"

# Remove last ".git"
repo="${repo/.git/}"

open $repo
