#!/usr/bin/env bash

msg='Initialize project'

echo -e "[x] Run: git init"
git init

echo -e "[x] Run: git add ."
git add .

echo -e "[x] Run: git commit -am '$msg'"
git commit -am "$msg"

echo -e "[x] Run: git branch -m main"
git branch -m main
