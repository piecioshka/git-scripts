#!/usr/bin/env bash

source __shared.sh

msg='Initialize project'

printCommand "git init"
git init

printCommand "git add . -v"
git add . -v

printCommand "git commit -am '$msg'"
git commit -am "$msg"

printCommand "git branch -m main"
git branch -m main
